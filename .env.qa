# QA (local testing) environment — loaded by Vite when run with `--mode qa`.
# This environment is fully isolated from production (Fly).
VITE_APP_ENV=qa
VITE_DEV_PORT=5174
# QA frontend talks directly to the QA API server (CORS is enabled server-side).
VITE_API_URL=http://localhost:3002/api
